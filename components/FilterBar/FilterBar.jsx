import React from "react";
import styles from "./FilterBar.module.css";

const filterBar = () => {
  return <div className={styles.filterBarContainer}>filterBar</div>;
};

export default filterBar;
